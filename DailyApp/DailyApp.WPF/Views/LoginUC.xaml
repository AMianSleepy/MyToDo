<UserControl x:Class="DailyApp.WPF.Views.LoginUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DailyApp.WPF.Views"
             mc:Ignorable="d"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:pwdExtend="clr-namespace:DailyApp.WPF.Extensions"
             Height="400" Width="800">
    <!--第三方素材库-->
    <prism:Dialog.WindowStyle>
        <Style TargetType="Window">
            <Setter Property="SizeToContent" Value="WidthAndHeight"/>
            <Setter Property="ResizeMode" Value="NoResize"/>
            <Setter Property="prism:Dialog.WindowStartupLocation" Value="CenterScreen"/>
        </Style>
    </prism:Dialog.WindowStyle>

    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.5*"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Image Margin="0,0,0,0" Source="/Images/login.png" Stretch="Fill"/>

        <!--显示提示信息-->
        <md:Snackbar x:Name="RegLoginBar" Grid.ColumnSpan="2" MessageQueue="{md:MessageQueue}"></md:Snackbar>

        <!--<TextBlock Text="这是登录界面" FontSize="30"></TextBlock>
        <Button Content="登录" Width="100" Height="50" FontSize="30" Command="{Binding LoginCmm}"/>-->

        <md:Transitioner Grid.Column="1" SelectedIndex="{Binding SelectedIndex, FallbackValue=0}">
            <!--第一块 登录-->
            <md:TransitionerSlide>
                <DockPanel Margin="15" VerticalAlignment="Center">
                    
                    <DockPanel.InputBindings>
                        <KeyBinding Key="Enter" Command="{Binding LoginCmm}"/>
                    </DockPanel.InputBindings>

                    <TextBlock Text="欢迎使用" FontWeight="Bold" FontSize="22" DockPanel.Dock="Top" Margin="0,15" HorizontalAlignment="Center"></TextBlock>
                    <TextBox md:HintAssist.Hint="请输入账号" DockPanel.Dock="Top" Margin="0,15" Text="{Binding Account}"></TextBox>
                    <PasswordBox md:HintAssist.Hint="请输入密码" DockPanel.Dock="Top"  Margin="0,15" 
                                 pwdExtend:PasswordBoxExtend.Pwd="{Binding Pwd,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                        <i:Interaction.Behaviors>
                            <pwdExtend:PasswordBoxBehavior></pwdExtend:PasswordBoxBehavior>
                        </i:Interaction.Behaviors>
                    </PasswordBox>

                    <Button Content="登录" DockPanel.Dock="Top" Margin="0,15" Command="{Binding LoginCmm}"/>

                    <DockPanel Margin="0.5" LastChildFill="False">
                        <TextBlock Text="注册账号">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="MouseLeftButtonDown">
                                    <i:InvokeCommandAction Command="{Binding ShowRegInfoCmm}"></i:InvokeCommandAction>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </TextBlock>

                        <TextBlock Text="忘记密码" DockPanel.Dock="Right"></TextBlock>
                    </DockPanel>
                </DockPanel>
            </md:TransitionerSlide>

            <!--第二块 注册-->
            <md:TransitionerSlide>
                <DockPanel Margin="15" VerticalAlignment="Center">
                    <TextBlock Text="注册账号" FontWeight="Bold" FontSize="22" DockPanel.Dock="Top" Margin="0,15" HorizontalAlignment="Center"></TextBlock>
                    <TextBox md:HintAssist.Hint="请输入名称" DockPanel.Dock="Top" Margin="0,10" Text="{Binding AccountInfoDTO.Name}"></TextBox>
                    <TextBox md:HintAssist.Hint="请输入账号" DockPanel.Dock="Top" Margin="0,10" Text="{Binding AccountInfoDTO.Account}"></TextBox>
                    <PasswordBox md:HintAssist.Hint="请输入密码" DockPanel.Dock="Top"  Margin="0,10" 
                                 pwdExtend:PasswordBoxExtend.Pwd="{Binding AccountInfoDTO.Pwd,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                        <i:Interaction.Behaviors>
                            <pwdExtend:PasswordBoxBehavior></pwdExtend:PasswordBoxBehavior>
                        </i:Interaction.Behaviors>
                    </PasswordBox>
                    <PasswordBox md:HintAssist.Hint="请再次输入密码" DockPanel.Dock="Top"  Margin="0,10" 
                                 pwdExtend:PasswordBoxExtend.Pwd="{Binding AccountInfoDTO.ConfirmPwd,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                        <i:Interaction.Behaviors>
                            <pwdExtend:PasswordBoxBehavior></pwdExtend:PasswordBoxBehavior>
                        </i:Interaction.Behaviors>
                    </PasswordBox>
                    <Button Content="注册账号" DockPanel.Dock="Top" Command="{Binding RegCmm}"></Button>
                    <Button Content="返回登录" DockPanel.Dock="Top" Margin="0,10" Command="{Binding ShowLoginInfoCmm}" Style="{StaticResource MaterialDesignOutlinedLightButton}"></Button>
                </DockPanel>
            </md:TransitionerSlide>
        </md:Transitioner>
    </Grid>
</UserControl>
